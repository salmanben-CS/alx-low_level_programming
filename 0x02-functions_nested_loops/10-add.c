#include<stdio.h>
#include "main.h"
/**
* add -add two numbers
* Return: the sum of the two numbers
* @a:the first parameter added
* @b:the second parameter added
*/
int add(int a, int b)
{
int sum = a + b;
return (sum);
}
