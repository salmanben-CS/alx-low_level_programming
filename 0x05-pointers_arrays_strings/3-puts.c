#include <stdio.h>
#include "main.h"
/**
* _puts -prints string to the stdout
*@s: is a pointer points on the string that will be printed
* Return:void
*/
void _puts(char *str)
{
int i;
for (i = 0; s[i] != '\0'; i++)
_putchar(s[i]);
}
